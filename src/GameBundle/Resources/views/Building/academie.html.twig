{% extends 'GameBundle::index.html.twig' %}

{% block title %}{{ parent() }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}
{% endblock %}

{% block body %}
    Vous avez actuellement {{ infTown }} genin(s).<br>
    Et {{ infInBuild }} en formation.
    <br><br>
    Genin : <br>
    <table>
        <tr>
            <td>Attaque</td><td>Défense</td><td>Pierre</td><td>Bois</td><td>Fer</td><td>Nourriture</td><td>Temps formation (mn)</td>
        </tr>
        <tr>
            <td>{{ genin.atk }}</td><td>{{ genin.defense }}</td>
            {% for r in genin.ressources %}
                <td>{{ r.nb }}</td>
            {% endfor %}
        <td>{{ genin.time }}</td>
        </tr>
    </table>
    Recrutez en ici :

    {{ form_start(form) }}<br>
    {{ form_widget(form) }}<br>
    <input type="submit" class="button" value="Submit" />
    {{ form_end(form) }}

{% endblock %}