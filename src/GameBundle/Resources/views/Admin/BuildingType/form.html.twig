{% extends 'GameBundle::Admin/index.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block sidebar %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ title }} :<br>
    {% if edit and buildingType.image %}
        <img src="{{ asset(buildingType.image.webPath) }}" alt="{{ buildingType.image.alt }}"/>
    {% endif %}
    {{ form_start(form) }}<br>
    {{ form_widget(form) }}<br>
    <input type="submit" class="button" value="Submit" />
    {{ form_end(form) }}
{% endblock %}